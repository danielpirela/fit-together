// Spring animation hook with reusable presets

export { useSpringAnimation, usePressScale, useBounceAnimation } from './use-spring'

// Entry animation hooks
export {
  useEntryAnimation,
  useListItemAnimation,
  useFadeIn,
  useSlideIn,
  createStaggerAnimation,
} from './use-entry'

// Press animation hooks
export { usePressAnimation, useButtonPress, useRippleEffect } from './use-press'

// Shake animation hook
export { useShake, useRotationShake } from './use-shake'
